<!--
    Protected Resource Metadata (RFC 9728) policy.
    Returns PRM JSON for OAuth 2.0 resource discovery.
    Anonymous access — no authentication required.
-->
<policies>
    <inbound>
        <return-response>
            <set-status code="200" reason="OK" />
            <set-header name="Content-Type" exists-action="override">
                <value>application/json</value>
            </set-header>
            <set-header name="Cache-Control" exists-action="override">
                <value>public, max-age=3600</value>
            </set-header>
            <set-body>@{
                return JsonConvert.SerializeObject(new {
                    resource = "{{APIMGatewayURL}}/{{McpApiPath}}",
                    authorization_servers = new[] {
                        $"https://login.microsoftonline.com/{{entraid-tenant}}/v2.0"
                    },
                    bearer_methods_supported = new[] {
                        "header"
                    },
                    scopes_supported = new[] {
                        "api://{{obo-client-id}}/access_mcp"
                    }
                });
            }</set-body>
        </return-response>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
